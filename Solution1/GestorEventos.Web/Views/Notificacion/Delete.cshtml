@model GestorEventos.Web.Models.NotificacionViewModel

<div class="card text-primary">
    <div class="card-header text-center">
        <h5>Eliminar Notificación</h5>
    </div>
    <div class="card-body">
        <p class="text-danger">¿Estás seguro de que deseas eliminar esta notificación?</p>
        <dl class="row">
            <dt class="col-sm-2 text-center text-dark">
                @Html.DisplayNameFor(model => model.Mensaje)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Mensaje)
            </dd>

            <dt class="col-sm-2 text-center text-dark">
                @Html.DisplayNameFor(model => model.FechaEnvio)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.FechaEnvio)
            </dd>

            <dt class="col-sm-2 text-center text-dark">
                @Html.DisplayNameFor(model => model.ParticipanteId)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.ParticipanteId)
            </dd>
        </dl>
    </div>
    <div class="card-footer text-center">
        <form asp-action="Delete" onsubmit="return confirm('¿Estás seguro de que deseas eliminar esta notificación?');">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Eliminar" class="btn btn-outline-danger btn-sm" />
            <a asp-action="Index" class="btn btn-outline-secondary btn-sm">Cancelar</a>
        </form>
    </div>
</div>
